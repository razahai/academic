cmake_minimum_required(VERSION 2.8)
project( rcv )
find_package( OpenCV REQUIRED )
message("OpenCV version: ${OpenCV_VERSION}")
add_executable( mainlab mainlab.cpp )
target_link_libraries( mainlab ${OpenCV_LIBS} )
